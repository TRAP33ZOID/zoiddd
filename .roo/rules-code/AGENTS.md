# Project Coding Rules (Non-Obvious Only)

- **Class Merging:** Always use the `cn()` utility function from `@/lib/utils.ts` when combining or conditionally applying Tailwind CSS classes.
- **Absolute Imports:** Use the `@/` alias for absolute imports from the project root.